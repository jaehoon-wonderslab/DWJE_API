-- =====================================================================================
--  V37 되돌리기 — 용어 대소문자 중복 방지 인덱스 제거 (2026-09-16)
--
--  V37__gls_term_case_insensitive_unique.sql 이 만든 ax.uq_gls_term_lower 를 지운다.
--  인덱스 하나만 다루므로 데이터는 그대로다 — 덤프를 먼저 돌릴 필요가 없다.
--
--  [되돌리면 다시 열리는 것]
--  'can' 과 'CAN' 을 각각 등록할 수 있게 된다. uq_tb_gls_term (term 원문) 은 남아 있으므로
--  똑같은 표기의 중복만 계속 막힌다.
--
--  [API 는 그대로 둬도 된다]
--  GlossaryService 의 사전 검사는 lower(btrim()) 로 조회해 409 를 주므로 인덱스가 없어도
--  정상 경로는 그대로 막힌다. 인덱스가 없으면 동시 요청이 겹칠 때만 두 행이 들어갈 수 있다.
--  되돌린 상태를 오래 두면 GlossaryDuplicateSqlTest.uniqueIndexExists 가 실패한다 —
--  일시적으로 되돌리는 것이 아니라면 그 테스트도 함께 정리한다.
--
--  [적용]
--  psql -U <user> -d <db> -v ON_ERROR_STOP=1 -f rollback/V37__down.sql
--  두 번 실행해도 안전하다.
-- =====================================================================================

DROP INDEX IF EXISTS ax.uq_gls_term_lower;
